version: '2'
services:
    app:
        build:
            context: .
            dockerfile: Dockerfile
        volumes:
            - .:/var/www/app
            - ./other:/etc/gocron
            - nodemodules:/var/www/app/node_modules

volumes:
    nodemodules: {}
