version: '2'
services:
    app:
        build:
            context: .
            dockerfile: Dockerfile
        volumes:
            - .:/var/www/app
            - ./other:/var/www/other
            - nodemodules:/var/www/app/node_modules

volumes:
    nodemodules: {}
